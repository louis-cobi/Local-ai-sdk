# Versioning note

This documentation targets **V0.0.0 as a proxy baseline**.

Because no historical `0.0.0` git tag/commit was provided, the reference surface is the repository state at the time this documentation was generated.

## Guarantee scope

- Public exports and signatures are documented from package entry points.
- Behavioral descriptions reflect current implementation of the engine/tooling/provider adapters.
- If a true historical `0.0.0` tag is later introduced, this site should be regenerated from that exact reference to produce a strict historical snapshot.
